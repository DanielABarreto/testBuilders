const imageError = require('../assets/images/error.jpeg');
const imageErrorLocation = require('../assets/images/errorLocation.png');

const images = {
  imageError,
  imageErrorLocation,
}

export default images

